# Baked into `npm run build` for GitHub Pages — points at the Render game server.
# Deploy server once: https://dashboard.render.com → Blueprint → this repo (render.yaml).
VITE_SOCKET_URL=https://g-play-socket.onrender.com
